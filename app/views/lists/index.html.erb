<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

  <% @lists.each do |list| %>
    
    <p>タイトル</p>
    <span><%= link_to list.title, "/lists/#{list.id}" %></span>
    
  <% end %>
